[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — single-page app routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
